{
  "name": "cybersecurity-path-app",
  "version": "1.0.0",
  "homepage": ".",
  "description": "A web application for a cybersecurity path.",
  "scripts": {
    "start": "react-scripts start",
    "build": "cp docs/index.html public/index.html && react-scripts build && mv build/* docs/ && rm -rf build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.18.0",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
