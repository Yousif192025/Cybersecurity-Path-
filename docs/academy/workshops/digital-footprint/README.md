# Future workshop — not yet implemented

This folder is a reserved placeholder for a future workshop built on top of
the shared Workshop Engine (`../../engine/`). It intentionally contains no
content yet — see the main project README for how to build a new workshop
here (`workshop.json` + `questions.json` + `index.html`), following the same
pattern as `../deleted-data/`.
